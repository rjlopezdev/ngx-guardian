import { ShowIfGrantedDirective } from './show-if-granted.directive';

describe('ShowIfGrantedDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfGrantedDirective();
    expect(directive).toBeTruthy();
  });
});
